fastapi==0.116.1
uvicorn==0.35.0
python-multipart==0.0.20
deepface==0.0.95
tf-keras==2.21.0
opencv-python-headless==4.12.0.88
numpy==2.2.6
# CPU-only torch for DeepFace anti-spoofing (MiniFASNet).
# Use the CPU wheel index to avoid pulling the ~2GB CUDA build.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.4.1
